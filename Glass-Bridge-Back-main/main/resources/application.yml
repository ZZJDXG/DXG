server:
  port: 8080

spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/oa?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: 20040329hjsfw
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.cj.jdbc.Driver

  # ✅ 统一把 Date/Timestamp 输出成 yyyy-MM-dd（不带时间）
  jackson:
    date-format: yyyy-MM-dd
    time-zone: Asia/Shanghai

  thymeleaf:
    cache: false
    mode: LEGACYHTML5
    encoding: UTF-8
    servlet:
      content-type: text/html

mybatis:
  type-aliases-package: cn.yonyong.pojo
  mapper-locations: classpath:mapper/*.xml
